# -*- mode: snippet -*-
# name: meta attribute
# key: meta
# --

meta = {
  description = "$1";
  homepage = "$2";
  license = lib.licenses.$3;
  maintainers = with lib.maintainers; [ ludovicopiero ];
  platforms = lib.platforms.$4;
}; $5